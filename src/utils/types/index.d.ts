interface ITheme {
    colors: {
        primaryColor: string,
        secondaryColor: string,
        terciaryColor: string,
        buttonColor: string
    },
    fonts: {
        primaryFont: string
    }
}