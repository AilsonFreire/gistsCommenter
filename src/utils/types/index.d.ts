interface ITheme {
    colors: {
        buttonColor: string,
        primaryColor: string,
        secondaryColor: string,
        terciaryColor: string
    },
    fonts: {
        primaryFont: string
    }
}